
import { eventCaptureOptionDefaults } from '../options/event-capture-options-defaults';

export class EventCapture {

  // private eventCaptureOptions: EventCaptureOptions;

  constructor(selection: d3.Selection<SVGGElement, unknown, null, undefined>, options?: any) {
      // this.eventCaptureOptions = {
      //   ...eventCaptureOptionDefaults,
      //   ...options
      // };

      // console.log(this.eventCaptureOptions);

  }
}
